#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Describtion: program that prints 0-9
 * Return: 0 Always (Success)
*/
int main(void)
{
int digit = 0;
while (digit <= 9)
{
printf("%i, digit");
digit++;
}
printf("\n");
return (0);
}
